{
	"name": "ircd-base",
	"version": "1.4.1",
	"description": "A bare-bones and extensible embedded IRC daemon toolkit for NodeJS",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "prettier --write .",
		"check": "tsc --noEmit",
		"build": "tsc",
		"doPublish": "npm run build && npm publish"
	},
	"keywords": [
		"ircd",
		"api",
		"base",
		"irc"
	],
	"author": "Termer",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.4.2",
		"prettier": "^3.3.3"
	}
}
